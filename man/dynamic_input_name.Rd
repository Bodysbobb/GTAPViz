% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GTAP_Dynamic.R, R/GTAPauto.R
\name{dynamic_input_name}
\alias{dynamic_input_name}
\title{Generate Dynamic Input Mapping for GTAP Simulations}
\usage{
dynamic_input_name(
  prefix = NULL,
  suffix = NULL,
  pattern = NULL,
  increment = 1,
  separator = "-"
)

dynamic_input_name(
  prefix = NULL,
  suffix = NULL,
  pattern = NULL,
  increment = 1,
  separator = "-"
)
}
\arguments{
\item{prefix}{Character. Optional prefix to prepend to the generated names. Default is NULL.}

\item{suffix}{Character. Optional suffix to append to the generated names. Default is NULL.}

\item{pattern}{Character, numeric vector, or sequence definition. Defines a sequence of values to include in the name.
- If character with format `"start:end"`, generates a numeric sequence from `start` to `end` (inclusive).
- If numeric vector of length 2, treats as `c(start, end)` for sequence generation.
- If a vector (numeric or character), uses the provided values as-is.
- If NULL, only prefix and suffix are used.}

\item{increment}{Numeric. Step size for sequence generation when `pattern` defines a numeric range. Default is 1.}

\item{separator}{Character. String used to separate components in the generated names. Default is "-".}

\item{type}{Naming pattern}

\item{base}{Identifiers for baseline cases}

\item{pol}{Identifiers for policy cases}

\item{base_rerun}{Identifiers for baseline rerun cases}

\item{other}{Identifiers for other case types}

\item{period_pattern}{Creates period ranges between patterns}

\item{period_prefix}{Prefix for period names}

\item{output}{Name for result variable}
}
\value{
A data frame mapping experiments to cases and periods

A data frame mapping experiments to cases and periods.

A character vector containing the generated names based on the provided inputs.
}
\description{
Creates a mapping data frame of simulation experiments to cases and periods
for dynamic GE model analysis.

Constructs dynamic input names by combining a prefix, suffix, and a pattern sequence with a customizable separator.
This function allows flexible naming patterns for automation and structured naming conventions.
}
\examples{
# Generate names with a numeric sequence
dynamic_input_name(prefix = "Run", pattern = "1:5")

# Generate names with a suffix
dynamic_input_name(pattern = 1:3, suffix = "test")

# Generate names with a custom separator
dynamic_input_name(prefix = "Data", pattern = "10:50", increment = 10, separator = "_")

}
\author{
Pattawee Puangchit
}
