% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heat_map.R
\name{map_heatmap}
\alias{map_heatmap}
\title{Create Country/Region Heatmaps}
\usage{
map_heatmap(
  data,
  filter_var = NULL,
  iso_col = "REG",
  value_col = "Value",
  split_by = NULL,
  variable_col = "Variable",
  unit_col = "Unit",
  desc_col = "Description",
  map_region = "world",
  xlim = NULL,
  ylim = NULL,
  show_flags = FALSE,
  label_box_vars = NULL,
  map_value_var = NULL,
  aggregate_by = NULL,
  aggregate_fun = "mean",
  var_name_by_description = FALSE,
  add_var_info = FALSE,
  common_scale = FALSE,
  output_path = NULL,
  export_picture = TRUE,
  export_as_pdf = FALSE,
  export_config = NULL,
  map_style_config = NULL,
  title_format = NULL,
  show_unmatched_iso = TRUE
)
}
\arguments{
\item{data}{Data frame or list. GTAP data with country codes and values}

\item{filter_var}{NULL, vector, data frame, or named list specifying filtering conditions}

\item{iso_col}{Character. Column with ISO3 country codes. Default: \code{"REG"}}

\item{value_col}{Character. Column with numeric values. Default: \code{"Value"}}

\item{split_by}{Character or vector. Column(s) to create separate maps. Default: \code{NULL}}

\item{variable_col}{Character. Column with variable codes. Default: \code{"Variable"}}

\item{unit_col}{Character. Column with units. Default: \code{"Unit"}}

\item{desc_col}{Character. Column with descriptions. Default: \code{"Description"}

\strong{Map Region Settings}}

\item{map_region}{Character or vector. Region to display. Default: \code{"world"}

\strong{Supported regions:}
\itemize{
\item \strong{Continents:} \code{"world"} (default), \code{"asia"}, \code{"europe"}, \code{"africa"}, \code{"americas"}, \code{"north_america"}, \code{"south_america"}, \code{"oceania"}
\item \strong{Sub-regions:} \code{"asean"} (or \code{"southeast_asia"}, \code{"seasia"}), \code{"east_asia"}, \code{"south_asia"}, \code{"central_asia"}
\item \strong{Middle East/Africa:} \code{"mena"} (or \code{"middle_east"}), \code{"gcc"} (Gulf Cooperation Council), \code{"north_africa"}, \code{"sub_saharan_africa"}
\item \strong{Europe:} \code{"eu"} (or \code{"european_union"}), \code{"western_europe"}, \code{"eastern_europe"}
\item \strong{Americas:} \code{"latin_america"}, \code{"caribbean"}, \code{"nafta"} (or \code{"usmca"}), \code{"mercosur"}
\item \strong{Pacific:} \code{"pacific"}
\item \strong{Custom:} Vector of ISO3 codes (e.g., \code{c("USA", "CAN", "MEX")})
}}

\item{xlim}{Numeric vector. Longitude limits \code{c(min, max)}. Default: \code{NULL} (auto from region)}

\item{ylim}{Numeric vector. Latitude limits \code{c(min, max)}. Default: \code{NULL}

\strong{Label Box Settings}}

\item{show_flags}{Logical. Display country flags. Default: \code{FALSE}}

\item{label_box_vars}{Data frame or NULL. Variables to show in boxes. Create with
\code{\link{create_label_box_vars}}. If \code{NULL}, no label boxes shown. Default: \code{NULL}}

\item{map_value_var}{Character or NULL. Variable for map coloring. If \code{NULL}, uses first
variable or all data. Default: \code{NULL}

\strong{Aggregation}}

\item{aggregate_by}{Character or vector. Columns to aggregate by. Default: \code{NULL}}

\item{aggregate_fun}{Character. Aggregation function: \code{"mean"}, \code{"sum"}, \code{"median"}. Default: \code{"mean"}

\strong{Variable Display}}

\item{var_name_by_description}{Logical. Use descriptions in titles. Default: \code{FALSE}}

\item{add_var_info}{Logical. Append variable codes. Default: \code{FALSE}}

\item{common_scale}{Logical. Use unified color scale across split maps. When \code{TRUE} and
\code{split_by} is used, all maps share the same color scale based on global min/max values,
making colors comparable across maps. When \code{FALSE}, each map uses its own min/max for
color scaling. Default: \code{FALSE}

\strong{Export Settings}}

\item{output_path}{Character. Directory to save maps. Default: \code{NULL}}

\item{export_picture}{Logical. Export as PNG. Default: \code{TRUE}}

\item{export_as_pdf}{Logical or \code{"merged"}. Export as PDF. Default: \code{FALSE}}

\item{export_config}{List. Export options. See \code{\link{create_export_config}}

\strong{Styling}}

\item{map_style_config}{List. Map appearance. See \code{\link{create_map_style}}}

\item{title_format}{List or NULL. Title formatting. See \code{\link{create_title_format}}}

\item{show_unmatched_iso}{Logical. Warn about unmatched ISO codes. Default: \code{TRUE}}
}
\value{
List with \code{plots} (ggplot objects) and \code{unmatched_iso} (character vector)
}
\description{
Creates geographical heatmaps for GTAP data with optional label boxes.

\strong{Input Data}
}
\examples{
\dontrun{
# Simple map without labels
map_heatmap(
  data = gtap_data,
  iso_col = "Region",
  value_col = "Value",
  split_by = "Case"
)

# Map with label boxes showing multiple variables
label_vars <- create_label_box_vars(
  variable = c("EV", "qgdp", "qpriv"),
  label = c("Welfare", "GDP", "Consumption")
)

map_heatmap(
  data = gtap_data,
  iso_col = "Region",
  map_value_var = "qgdp",
  label_box_vars = label_vars,
  show_flags = TRUE,
  split_by = "Case"
)

# Use unified color scale across all maps for comparison
map_heatmap(
  data = gtap_data,
  iso_col = "Region",
  value_col = "Value",
  split_by = "Case",
  common_scale = TRUE  # All maps use same color scale
)
}
}
\author{
Pattawee Puangchit
}
