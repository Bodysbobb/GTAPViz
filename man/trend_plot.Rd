% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{trend_plot}
\alias{trend_plot}
\title{Create Time-Series Trend Plots from GTAP Data}
\usage{
trend_plot(
  data,
  filter_var = NULL,
  panel_var,
  group_by,
  split_by = NULL,
  variable_col = "Variable",
  unit_col = "Unit",
  desc_col = "Description",
  invert_pane = FALSE,
  separate_figure = FALSE,
  var_name_by_description = FALSE,
  add_var_info = FALSE,
  output_path = NULL,
  export_picture = TRUE,
  export_as_pdf = FALSE,
  export_config = NULL,
  plot_style_config = NULL,
  line_size = 1.5,
  add_points = TRUE,
  point_size = 3,
  add_smooth = FALSE,
  smooth_method = "loess"
)
}
\arguments{
\item{data}{A data frame or a list of data frames containing GTAP results.}

\item{filter_var}{Vector or data frame. If a vector, filters the values in `group_by`.
If a data frame, filters `variable_col` based on matching values.}

\item{panel_var}{Character. Column containing time variable for x-axis (e.g., "Year").}

\item{group_by}{Character. Column for grouping lines (e.g., "REG" or "Country").}

\item{split_by}{Character or vector. Column name(s) for data splitting (e.g., "COMM").
Set to NULL for no splitting, suitable for macro-level analysis.}

\item{variable_col}{Character. Column containing variable identifiers (default: "Variable").}

\item{unit_col}{Character. Column containing unit information (default: "Unit").}

\item{desc_col}{Character. Column containing variable descriptions (default: "Description").}

\item{invert_pane}{Logical. If TRUE, creates horizontal trend lines (default: FALSE).}

\item{separate_figure}{Logical. If TRUE, generates separate figures per variable (default: FALSE).}

\item{var_name_by_description}{Logical. If TRUE, uses descriptions instead of variable codes (default: FALSE).}

\item{add_var_info}{Logical. If TRUE, adds variable code in parentheses after description (default: FALSE).}

\item{output_path}{Character. Directory path for saving output files. If NULL, plots are only returned.}

\item{export_picture}{Logical. If TRUE, exports plots as image files (default: TRUE).}

\item{export_as_pdf}{Logical or "merged". If TRUE, exports separate PDFs; if "merged", creates a multi-page PDF.}

\item{export_config}{List. Export settings like width, height, and file name.}

\item{plot_style_config}{List. Custom plot styles (see `?get_plot_style_config`).}

\item{line_size}{Numeric. Thickness of trend lines (default: 1.5).}

\item{add_points}{Logical. If TRUE, adds point markers at each data point (default: TRUE).}

\item{point_size}{Numeric. Size of point markers (default: 3).}

\item{add_smooth}{Logical. If TRUE, adds smoothed trend lines (default: FALSE).}

\item{smooth_method}{Character. Method for smoothing ("loess", "lm", etc.) (default: "loess").}
}
\value{
A list of ggplot2 objects (invisibly) containing the generated plots.
}
\description{
Generates line plots to visualize trends over time across multiple dimensions,
with flexible grouping, splitting, and styling options.
}
\examples{
\donttest{
# Basic trend plot
trend_plot(
  data = time_series_data,
  panel_var = "Year",
  group_by = "Region",
  output_path = "/path/to/output"
)

# Advanced configuration
trend_plot(
  data = time_series_data,
  panel_var = "Year",
  group_by = "Country",
  split_by = "Variable",
  line_size = 1.2,
  add_points = TRUE,
  add_smooth = TRUE,
  smooth_method = "lm",
  var_name_by_description = TRUE,
  plot_style_config = list(
    color_tone = "economic",
    show_legend = TRUE,
    legend_position = "bottom",
    show_zero_line = TRUE
  )
)
}
}
\seealso{
\code{\link{get_plot_style_config}}, \code{\link{get_export_config}}, \code{\link{comparison_plot}}, \code{\link{detail_plot}}, \code{\link{stack_plot}}
}
\author{
Pattawee Puangchit
}
