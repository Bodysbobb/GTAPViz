% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{comparison_plot}
\alias{comparison_plot}
\title{Create Comparative Bar Charts from HAR and SL4 Data}
\usage{
comparison_plot(
  data,
  filter_var = NULL,
  x_axis_from,
  split_by = NULL,
  panel_var = "Experiment",
  variable_col = "Variable",
  unit_col = "Unit",
  desc_col = "Description",
  invert_pane = FALSE,
  separate_figure = FALSE,
  var_name_by_description = FALSE,
  add_var_info = FALSE,
  output_dir = NULL,
  width = NULL,
  height = NULL,
  plot_style_config = NULL
)
}
\arguments{
\item{data}{A data frame or a list of data frames containing GTAP results.}

\item{filter_var}{Vector or data frame. If a vector, filters the values in \code{x_axis_from}.
If a data frame, filters \code{value_col} based on matching \code{variable_col} values.}

\item{x_axis_from}{Character. Column name for x-axis categories (e.g., "REG", "Sector").}

\item{split_by}{Character or vector. Column name(s) for data splitting (e.g., "COMM", "REG", "ACTS").
Set to NULL for no splitting, which is suitable for macro-level analysis (i.e., aggregated values without additional dimensions).}

\item{panel_var}{Character. Column for panel facets (default: "Experiment").}

\item{variable_col}{Character. Column containing variable identifiers (default: "Variable").}

\item{unit_col}{Character. Column containing unit information (default: "Unit").}

\item{desc_col}{Character. Column containing variable descriptions (default: "Description").}

\item{invert_pane}{Logical. If TRUE, creates horizontal bars instead of vertical ones (default: FALSE).}

\item{separate_figure}{Logical. If TRUE, generates separate figures per panel value (default: FALSE).}

\item{var_name_by_description}{Logical. If TRUE, uses descriptions instead of variable codes in titles (default: FALSE).}

\item{add_var_info}{Logical. If TRUE, adds the variable code in parentheses after the description (default: FALSE).}

\item{output_dir}{Character. Directory path to save plots as \code{.png} files. If NULL, plots are only returned in R without saving.}

\item{width}{Numeric. Width of the output figure in inches. If NULL, it is calculated automatically.}

\item{height}{Numeric. Height of the output figure in inches. If NULL, it is calculated automatically.}

\item{plot_style_config}{List. Custom style configuration for plots. If NULL, defaults from \code{get_plot_style_config("comparison")} are applied.}
}
\value{
A ggplot2 object for a single plot or a list of ggplot2 objects for multiple plots.
}
\description{
Generates comparative bar charts using GTAP data, allowing multiple visualization options
such as panel facets, split grouping, and customizable styles.
}
\details{
This function allows for extensive customization of plots through the \code{plot_style_config} parameter,
which integrates ggplot2 configurations.
To view and adjust available style options, use:
\code{get_plot_style_config("comparison", as_dataframe = TRUE)}.
}
\examples{
\dontrun{
# Basic usage with data frame
p1 <- comparison_plot(
  data = gtap_results,
  x_axis_from = "REG",
  panel_var = "Experiment"
)

# Split by commodity with custom styling
p2 <- comparison_plot(
  data = gtap_results,
  x_axis_from = "REG",
  split_by = "COMM",
  panel_var = "Experiment",
  var_name_by_description = TRUE,
  plot_style_config = list(
    color_tone = "economic",
    title_size = 16,
    show_grid_major_y = TRUE
  )
)

# Save plots to directory with inverted orientation
plots <- comparison_plot(
  data = gtap_results,
  x_axis_from = "REG",
  split_by = "COMM",
  invert_pane = TRUE,
  output_dir = "path/to/output/directory"
)
}

}
\seealso{
\code{\link{detail_plot}}, \code{\link{stack_plot}}, \code{\link{get_plot_style_config}}
}
\author{
Pattawee Puangchit
}
