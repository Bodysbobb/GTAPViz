% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{detail_plot}
\alias{detail_plot}
\title{Create Comprehensive Bar Charts from HAR and SL4 Data}
\usage{
detail_plot(
  data,
  filter_var = NULL,
  x_axis_from,
  split_by = NULL,
  panel_var = "Experiment",
  variable_col = "Variable",
  unit_col = "Unit",
  desc_col = "Description",
  var_name_by_description = FALSE,
  add_var_info = FALSE,
  output_path = NULL,
  export_picture = TRUE,
  export_as_pdf = FALSE,
  export_config = NULL,
  top_impact = NULL,
  separate_figure = FALSE,
  invert_pane = FALSE,
  plot_style_config = NULL
)
}
\arguments{
\item{data}{A data frame or a list of data frames containing GTAP results.}

\item{filter_var}{Vector or data frame. If a vector, filters the values in \code{x_axis_from}.
If a data frame, filters \code{value_col} based on matching \code{variable_col} values.}

\item{x_axis_from}{Character. Column name for x-axis categories (e.g., "REG", "Sector").}

\item{split_by}{Character or vector. Column name(s) for data splitting (e.g., "COMM", "REG", "ACTS").
Set to NULL for no splitting, suitable for macro-level analysis.}

\item{panel_var}{Character. Column for panel facets (default: "Experiment").}

\item{variable_col}{Character. Column containing variable identifiers (default: "Variable").}

\item{unit_col}{Character. Column containing unit information (default: "Unit").}

\item{desc_col}{Character. Column containing variable descriptions (default: "Description").}

\item{var_name_by_description}{Logical. If TRUE, uses descriptions instead of variable codes in titles (default: FALSE).}

\item{add_var_info}{Logical. If TRUE, adds the variable code in parentheses after the description (default: FALSE).}

\item{output_path}{Character. Directory path for saving output files. If NULL, plots are only returned in R.}

\item{export_picture}{Logical. If TRUE, exports plots as image files (default: TRUE).}

\item{export_as_pdf}{Logical or "merged". If TRUE, exports separate PDFs; if "merged", creates a multi-page PDF; if FALSE, skips PDF (default: FALSE).}

\item{export_config}{List. Export settings, including:
\itemize{
\item \code{width}: Output file width (in inches).
\item \code{height}: Output file height (in inches).
\item Additional settings—see \code{?get_export_config}.
}}

\item{top_impact}{Numeric or NULL. If specified, shows only the top N impactful values; NULL shows all values.}

\item{separate_figure}{Logical. If TRUE, generates separate figures per panel value (default: FALSE).}

\item{invert_pane}{Logical. If TRUE, creates horizontal bars instead of vertical ones (default: FALSE).}

\item{plot_style_config}{List. Custom plot styles—see \code{?get_plot_style_config}.}
}
\value{
A ggplot2 object for a single plot or a list of ggplot2 objects for multiple plots.
}
\description{
Generates detailed bar charts to visualize the distribution of impacts across multiple dimensions.
The function supports top impact filtering, color coding, and flexible visualization settings.
}
\examples{
\dontrun{
# Basic usage showing all impacts with export options
p1 <- detail_plot(
  data = gtap_results,
  x_axis_from = "REG",
  variable_col = "Variable",
  top_impact = NULL,
  output_path = "results/detail_plots",
  export_config = list(
    file_name = "regional_impacts",
    width = 14,
    height = 10
  )
)

# Show only top 10 impacts (balanced between positive and negative)
p2 <- detail_plot(
  data = gtap_results,
  x_axis_from = "Sector",
  split_by = "Region",
  panel_var = "Experiment",
  variable_col = "Variable",
  top_impact = 10,
  invert_pane = TRUE,
  export_as_pdf = TRUE
)
}

}
\seealso{
\code{\link{get_plot_style_config}}, \code{\link{get_export_config}}, \code{\link{comparison_plot}}, \code{\link{stack_plot}}
}
\author{
Pattawee Puangchit
}
